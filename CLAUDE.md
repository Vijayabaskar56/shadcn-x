# shadcn-x

This project's agent instructions live in @AGENTS.md — that file is the source

of truth (on-system rules, layout, verify commands). See also `CONTEXT.md`

(glossary) and `docs/adr/` (decisions).
---
title: Button
description: Displays a button or a component that looks like a button.
---

# Button

A themeable button built on the [Base UI](https://base-ui.com) button primitive
and styled with StyleX design tokens. Behavior and accessibility come from Base
UI; appearance comes from a closed set of typed `variant` and `size` props.
There is no `className` — customization happens through `variant`, `sx`, and
token re-theming.

<Box display="flex" alignItems="center" gap="m" flexWrap="wrap" padding="l" backgroundColor="background-card" borderRadius="m" border borderColor="border-primary">
  <Button>Default</Button>
  <Button variant="secondary">Secondary</Button>
  <Button variant="outline">Outline</Button>
  <Button variant="destructive">Delete</Button>
</Box>

## Usage

```tsx
import { Button } from "@/components/button"
```

```tsx
<Button variant="outline">Button</Button>
```

## Examples

### Size

Use the `size` prop to change the size of the button. Sizes range from `xs` to
`lg`, plus square `icon` sizes for icon-only buttons.

<Box display="flex" alignItems="center" gap="m" flexWrap="wrap" padding="l" backgroundColor="background-card" borderRadius="m" border borderColor="border-primary">
  <Button size="xs">Extra small</Button>
  <Button size="sm">Small</Button>
  <Button size="default">Default</Button>
  <Button size="lg">Large</Button>
  <Button size="icon" aria-label="Settings">⚙️</Button>
</Box>

```tsx
<Button size="xs">Extra small</Button>
<Button size="lg">Large</Button>
<Button size="icon" aria-label="Settings">⚙️</Button>
```

### Default

<Box padding="l" backgroundColor="background-card" borderRadius="m" border borderColor="border-primary">
  <Button>Button</Button>
</Box>

```tsx
<Button>Button</Button>
```

### Outline

<Box padding="l" backgroundColor="background-card" borderRadius="m" border borderColor="border-primary">
  <Button variant="outline">Button</Button>
</Box>

```tsx
<Button variant="outline">Button</Button>
```

### Secondary

<Box padding="l" backgroundColor="background-card" borderRadius="m" border borderColor="border-primary">
  <Button variant="secondary">Button</Button>
</Box>

```tsx
<Button variant="secondary">Button</Button>
```

### Ghost

<Box padding="l" backgroundColor="background-card" borderRadius="m" border borderColor="border-primary">
  <Button variant="ghost">Button</Button>
</Box>

```tsx
<Button variant="ghost">Button</Button>
```

### Destructive

The destructive variant uses a tinted surface with destructive-colored text.

<Box padding="l" backgroundColor="background-card" borderRadius="m" border borderColor="border-primary">
  <Button variant="destructive">Delete</Button>
</Box>

```tsx
<Button variant="destructive">Delete</Button>
```

### Link

<Box padding="l" backgroundColor="background-card" borderRadius="m" border borderColor="border-primary">
  <Button variant="link">Button</Button>
</Box>

```tsx
<Button variant="link">Button</Button>
```

### As Link

Use Base UI's `render` prop to give button styling to a router link. Note that
Base UI's `Button` applies `role="button"`, so prefer this for in-app navigation
controls rather than primary document links.

When the rendered element is not a native `<button>` (an `<a>`, a `div`, …),
pass `nativeButton={false}` so Base UI drops the native button semantics it
can't honor on a non-button host (form submission, the `disabled` attribute).
Without it Base UI logs a development warning.

```tsx
import { Link } from "@tanstack/react-router"

<Button
  variant="ghost"
  nativeButton={false}
  render={<Link to="/docs/$" params={{ _splat: "box" }} />}
>
  Docs
</Button>
```

## Customization

`Button` never accepts `className`. Customize it on-system, in order of reach:

1. **`variant` / `size` props** — the first-choice knobs (above).
2. **`sx`** — a typed, per-instance override that accepts StyleX styles only and
   merges last, so it wins. A raw utility string cannot enter it.
3. **`createTheme`** — re-skin a whole subtree by overriding token *values*,
   without touching the component.

```tsx
import * as stylex from "@stylexjs/stylex"

const styles = stylex.create({
  wide: { width: "100%" },
})

<Button sx={styles.wide}>Full width</Button>
```

## API Reference

| Prop      | Type                                                                                 | Default     |
| --------- | ------------------------------------------------------------------------------------ | ----------- |
| `variant` | `"default" \| "secondary" \| "outline" \| "ghost" \| "destructive" \| "link"`        | `"default"` |
| `size`    | `"default" \| "xs" \| "sm" \| "lg" \| "icon" \| "icon-xs" \| "icon-sm" \| "icon-lg"` | `"default"` |
| `sx`      | `StyleXStyles`                                                                        | —           |

All other [Base UI button props](https://base-ui.com/react/components/button)
(`disabled`, `onClick`, `render`, …) are forwarded.

> [!NOTE]
> Because `Button` has no `className`, an agent can only express on-system
> styling — the type system rejects everything else.

## RTL

<DirectionProvider dir="rtl">
  <Box dir="rtl" display="block" padding="l" backgroundColor="background-card" borderRadius="m" border borderColor="border-primary">
    <Button>
      <Icon name="ChevronRight" position="inline-end" flipRtl />
      متابعة
    </Button>
  </Box>
</DirectionProvider>

